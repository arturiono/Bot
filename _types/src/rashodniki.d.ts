import { MainContext } from '../types/types';
declare let MRashodniki: (msg: any, c: MainContext, editMode: Boolean, showInitialMessage: Boolean, end: () => any) => Promise<void>;
export default MRashodniki;
